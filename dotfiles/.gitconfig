[user]
    name = Massoud Boorboor
    email = boorboor@gmail.com
    signingkey = 3236520BC1654D472D5D5E28068CFD621D4263B1
[push]
    default = simple
[alias]
    d = !"git diff-index --quiet HEAD -- || clear; git --no-pager diff --patch-with-stat"
    s = status --short
    f = fetch --all --prune --progress --tags
    l = log --pretty=oneline --graph --abbrev-commit -20 --all
    ll = log --pretty=format:'%h || %G? || %ar || %aN || %s'
    lf = log --pretty=format:'%h %an %s ' --name-status
    search = log -S
    conflict = diff --name-only --diff-filter=U
    unstage = reset HEAD --
    last = log -1 HEAD
[core]
    excludesfile = $HOME/.gitignore_global
    editor = vim
    whitespace = trailing-space,space-before-tab
[color]
    ui = auto
[remote "origin"]
    prune = true
[commit]
    gpgsign = true
[merege]
    tool = vimdiff
    conflictstyle = diff3
    prompt = false
[pull]
    rebase = true
[credential]
    helper = cache --timeout 30000
[diff]
    tool = vimdiff
[apply]
    whitespace = fix
